pre-commit:
  parallel: true
  commands:
    lint:
      run: CI=true pnpm run lint
    type-check:
      run: CI=true pnpm run type-check

pre-push:
  commands:
    test:
      run: CI=true pnpm run test
